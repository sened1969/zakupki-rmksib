# 🔧 Предложения по улучшению ясности различий между анализами

## 📋 Резюме проблемы

`analyze_lot` и `analyze_documentation` могут показаться дублированием, хотя это два режима одного анализа:
- **Быстрый режим** (без документации)
- **Детальный режим** (с документацией)

## ✅ Предлагаемые изменения

### 1. Улучшить комментарии в промптах

#### В `ENHANCED_ANALYSIS_USER_TEMPLATE` добавить:

```python
<ЗАДАЧА>
Проведи БЫСТРЫЙ анализ целесообразности участия в конкурсной процедуре на основе данных карточки лота.
⚠️ ВНИМАНИЕ: Это предварительный анализ на основе ограниченной информации из карточки лота.
Для детального анализа требуется загрузить конкурсную документацию.

Нужно:
- Оценить ключевые параметры по 10-балльной шкале (0 = минимум риска/проблем, 10 = максимум риска/проблем).
- Рассчитать интегральную оценку риска по заданной формуле.
- Сформировать чёткую рекомендацию: участвовать / участвовать с осторожностью / не участвовать.
</ЗАДАЧА>
```

#### В `DOCUMENTATION_ANALYSIS_USER_TEMPLATE` добавить:

```python
<ЗАДАЧА>
Проведи ДЕТАЛЬНЫЙ анализ целесообразности участия в конкурсной процедуре на основе полной конкурсной документации.
✅ ВНИМАНИЕ: Это детальный анализ с извлечением точных данных из документации.
Используй всю доступную информацию для точной оценки.

1. Извлеки из документации:
   ...
```

### 2. Улучшить docstring функций

#### В `analyze_lot`:

```python
async def analyze_lot(lot: Lot, enhanced: bool = True, budget_min: int | None = None, budget_max: int | None = None) -> str:
	"""
	БЫСТРЫЙ анализ лота на основе данных карточки (без документации).
	
	Используется, когда конкурсная документация не загружена или недоступна.
	Анализирует общую информацию из карточки лота: название, описание, номенклатуру, бюджет.
	
	⚠️ Для детального анализа с точными расчётами используйте analyze_documentation().
	
	Args:
		lot: Объект лота для анализа
		enhanced: Если True, использует расширенный анализ с оценками по 10-балльной шкале
		budget_min: Минимальный бюджет из настроек пользователя (опционально)
		budget_max: Максимальный бюджет из настроек пользователя (опционально)
	
	Returns:
		Текст анализа лота (предварительная оценка)
	"""
```

#### В `analyze_documentation`:

```python
async def analyze_documentation(lot: Lot, documentation_text: str, budget_min: int | None = None, budget_max: int | None = None) -> str:
	"""
	ДЕТАЛЬНЫЙ анализ конкурсной документации для оценки рисков участия.
	
	Используется, когда конкурсная документация загружена и доступна.
	Извлекает точные данные из документации: количества товаров, технические требования,
	рассчитывает бюджет для каждого товара, оценивает сложность требований.
	
	✅ Это более точный анализ по сравнению с analyze_lot().
	
	Args:
		lot: Объект лота
		documentation_text: Извлеченный текст из документации
		budget_min: Минимальный бюджет из настроек пользователя (опционально)
		budget_max: Максимальный бюджет из настроек пользователя (опционально)
	
	Returns:
		Текст детального анализа документации с расчётами
	"""
```

### 3. Добавить комментарий в начало файла

```python
# =========================
# АРХИТЕКТУРА АНАЛИЗА ЗАКУПОК
# =========================
#
# В системе реализовано ДВА РЕЖИМА анализа одного лота:
#
# 1. analyze_lot() - БЫСТРЫЙ анализ по карточке лота
#    - Используется, когда документация НЕ загружена
#    - Анализирует общую информацию из карточки
#    - 4 параметра оценки, быстрая предварительная оценка
#
# 2. analyze_documentation() - ДЕТАЛЬНЫЙ анализ по документации
#    - Используется, когда документация ЗАГРУЖЕНА
#    - Извлекает точные данные из документации
#    - 5 параметров оценки, детальная оценка с расчётами
#
# Бот автоматически выбирает режим в зависимости от наличия документации.
# Это не дублирование, а два уровня детализации одного анализа.
#
# =========================
```

### 4. Обновить инструкцию для защиты

Добавить в `ИНСТРУКЦИЯ_ДЛЯ_ДЕМОНСТРАЦИИ.md` раздел:

```markdown
## ⚠️ Важно: Два режима анализа

**Не путайте:** `analyze_lot` и `analyze_documentation` — это не два разных анализа, 
а **два режима одного анализа** с разным уровнем детализации:

- **Быстрый режим** (`analyze_lot`) — когда документации нет
- **Детальный режим** (`analyze_documentation`) — когда документация есть

Бот автоматически выбирает режим в зависимости от наличия документации.
```

### 5. Улучшить формат ответа для ясности

#### В ответе `analyze_lot` добавить:

```
⚠️ ПРЕДВАРИТЕЛЬНЫЙ АНАЛИЗ
Это быстрый анализ на основе данных карточки лота.
Для детального анализа загрузите конкурсную документацию.

📊 ОЦЕНКИ ПО ПАРАМЕТРАМ:
...
```

#### В ответе `analyze_documentation` добавить:

```
✅ ДЕТАЛЬНЫЙ АНАЛИЗ
Это анализ на основе полной конкурсной документации.
Все расчёты выполнены на основе точных данных из документации.

📄 ИЗВЛЕЧЕННАЯ ИНФОРМАЦИЯ ИЗ ДОКУМЕНТАЦИИ:
...
```

## 🎯 Рекомендация

**Применить все изменения** для максимальной ясности:
1. ✅ Улучшить комментарии в промптах
2. ✅ Улучшить docstring функций
3. ✅ Добавить комментарий в начало файла
4. ✅ Обновить инструкцию для защиты
5. ✅ Улучшить формат ответа

Это сделает разницу очевидной и для куратора, и для будущих разработчиков.

## 📝 Альтернативный вариант (если времени мало)

Если нет времени на все изменения, минимум:
1. ✅ Добавить комментарий в начало файла (пункт 3)
2. ✅ Обновить инструкцию для защиты (пункт 4)

Этого будет достаточно, чтобы объяснить разницу на защите.

