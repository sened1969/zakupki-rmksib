<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# Перед защитой проекта куратор попросила подготовить ответы на следующий список вопросов, которые нужно будет показать на защите:

1. Сколько раз в алгоритме реализовано обращение к большой языковой модели?
2. Для каждого обращения показать в коде какая информация для анализа передается модели - источник этой информации?
3. Для каждого обращения показать промпт для модели.
4. Для каждого обращения показать результат генерации в телеграмм-боте.

Вопрос:

- целесообразно скорректировать промпт зашитый в файл perplexity.py для того чтобы явно было видно алгоритм анализа и удобно для дальнейшей доработки алгоритма? Или это будет ограничивать потенциал Perplexity?
- как изменить код чтобы наглядно продемонстрировать куратору ответы на её вопросы?

Оформи это в виде технического задания. Далее я передам это ТЗ в Cursor.

Ниже — готовое техническое задание, которое можно отдать в Cursor. Оно отвечает на вопросы куратора и включает доработки промптов и кода, чтобы всё было наглядно.[^1]

***

## ТЗ: Доработка модуля `perplexity.py` для демонстрации работы LLM

### 1. Цель работ

Обеспечить наглядную демонстрацию работы большой языковой модели (LLM, Perplexity) в проекте анализа закупок, чтобы на защите можно было:

- Чётко ответить, **сколько раз** и **где** вызывается LLM.[^1]
- Для КАЖДОГО вызова показать:
    - какие именно данные передаются модели и **откуда** они берутся (источник: объект `Lot`, текст документации, строка с названием товара и т.п.);[^1]
    - какой промпт формируется (system + user-сообщения);[^1]
    - какой результат модель вернула и как он отображается в Telegram‑боте.[^1]
- Сделать промпты **явно структурированными** и читаемыми, чтобы было видно алгоритм анализа и удобно развивать его дальше.

***

### 2. Анализ текущей реализации

В текущем модуле `perplexity.py` LLM вызывается через общую функцию `ask_perplexity`, а основные места использования такие:[^1]

1. `analyze_lot(lot: Lot, enhanced: bool = True, ...)`
    - Формирует промпт через `_lot_to_enhanced_prompt` (или `_lot_to_prompt` для базового анализа).
    - Передаёт в LLM данные лота: платформа, номер, название, заказчик, номенклатура, бюджет, статус, дедлайн, описание, а также настройки бюджетного диапазона и накладных расходов.[^1]
2. `analyze_documentation(lot: Lot, documentation_text: str, ...)`
    - Формирует промпт через `_documentation_to_prompt`.
    - Передаёт в LLM: данные лота (как выше) + текст конкурсной документации + указания по извлечению номенклатуры, расчёту бюджета и оценке рисков.[^1]
3. `search_suppliers_perplexity(product_name: str, max_suppliers: int = 20)`
    - Формирует промпт с названием товара и параметрами поиска поставщиков.[^1]
    - Передаёт в LLM только строку `product_name` и параметры `max_suppliers` в текстовом виде.[^1]

Все пять сценариев используют `call_llm_scenario()` (которая вызывает `ask_perplexity(messages=...)`) или напрямую `ask_perplexity()` и отправляют в Perplexity массив сообщений (`system` + `user`).[^1]

***

### 3. Требования к изменению промптов

#### 3.1. Общие требования

1. **Не ограничивать потенциал Perplexity**, а наоборот — сделать промпты более структурированными и понятными:
    - Явно перечислить шаги анализа (редкость, санкции, бюджет, другие факторы и т.п.).[^1]
    - Явно описать шкалы (0–10) и веса в интегральной оценке.[^1]
    - Сохранять свободу модели внутри этих рамок (не жёсткий алгоритм “if-else”, а именно методологию).[^1]
2. Разделить промпты на логические блоки с тегами:
    - `<ДАННЫЕ ЛОТА>`, `<ЗАДАЧА>`, `<ПАРАМЕТРЫ ДЛЯ ОЦЕНКИ>`, `<АЛГОРИТМ АНАЛИЗА>`, `<ФОРМАТ ОТВЕТА>`.[^1]
    - В блоке `<АЛГОРИТМ АНАЛИЗА>` явно описать логику оценки редкости, санкций, бюджета и других факторов, чтобы на защите можно было показать: «Вот как модель умеет думать».[^1]
3. Вынести большие текстовые куски промпта в **константы/шаблоны** (например, `ENHANCED_ANALYSIS_INSTRUCTIONS`, `DOCUMENTATION_ANALYSIS_INSTRUCTIONS`), чтобы код был читаемым и промпты было легко редактировать.[^1]

#### 3.2. Конкретные изменения

1. В `_lot_to_enhanced_prompt` добавить отдельный блок `<АЛГОРИТМ АНАЛИЗА>` примерно такого содержания (структурно, не дословно в ТЗ):
    - Как оценивать **редкость**: признаки массовых/стандартных товаров vs нишевых/нестандартных, влияние количества поставщиков, ГОСТ/нестандарт, спецматериалы, допуски.[^1]
    - Как оценивать **санкционный риск**: страна происхождения, технологичность (электроника/оборудование/сырьё), ключевые слова, признаки ВПК/двойного назначения, импортные компоненты.[^1]
    - Как сравнивать **бюджет**: диапазон, расчёт бюджета с накладными расходами, логика “бюджет слишком низкий/слишком высокий/в норме”.[^1]
    - Как учитывать **другие факторы**: сроки, логистика, репутация заказчика, условия оплаты, конкуренция.[^1]

Цель — чтобы куратор увидела, что алгоритм анализа фиксирован в виде текстовой методологии внутри промпта.
2. В `_documentation_to_prompt` аналогично усилить блок `<ПАРАМЕТРЫ ДЛЯ ОЦЕНКИ>` + `<РАСЧЕТ ИНТЕГРАЛЬНОЙ ОЦЕНКИ РИСКА>` и выделить явно веса факторов, как уже сделано, но структурированнее (каждый фактор — отдельная строка с весом).![^1]
3. В промпте `search_suppliers_perplexity` явно добавить блок `<АЛГОРИТМ ПОИСКА>`:
    - Сначала найти список реальных компаний.
    - Приоритет: российские поставщики + наличие сайта.
    - Ничего не придумывать (НИКАКИХ вымышленных контактов).
    - Строгий формат вывода.![^1]

***

### 4. Логирование и трассировка (ответы на вопросы куратора)

#### 4.1. Добавить уровни логирования для каждого вызова LLM

Требуется, чтобы по логам (и в коде) было наглядно видно:

1. **Сколько раз** вызывается LLM:
    - Каждый вызов `ask_perplexity` логируется на уровне `INFO` с указанием:
        - имени сценария (`analyze_lot`, `analyze_documentation`, `search_suppliers_perplexity`);
        - количества сообщений в `messages`;
        - длины user‑промпта (кол-во символов).![^1]
2. **Какие данные передаются** (источники):
    - Перед вызовом `ask_perplexity` в `analyze_lot` сделать лог на `DEBUG` с краткой сводкой:
        - `lot_id` / `lot_number`;
        - `platform_name`, `customer`, `budget`;
        - `nomenclature` (обрезать до разумной длины);
        - флаг `enhanced` и значения `budget_min`, `budget_max`.[^1]
    - Аналогично в `analyze_documentation`: логировать длину документации и первые N символов.[^1]
    - В `search_suppliers_perplexity`: логировать `product_name` и `max_suppliers`.[^1]
3. **Какой промпт уходит в модель**:
    - Перед `ask_perplexity` логировать **полный промпт** (обрезанный, если очень длинный) в человекочитаемом виде:
        - блок `SYSTEM:` — содержание `messages[^0]["content"];`
        - блок `USER:` — содержание `messages[^1]["content"]` (или всех user-сообщений).![^1]
4. **Какой результат вернула модель**:
    - После ответа LLM логировать:
        - первые N символов ответа;
        - длину ответа (кол-во символов/токенов).![^1]

Важно: Логи нужны именно для показа куратору: «Вот входные данные, вот промпт, вот ответ».

#### 4.2. Структурированная функция-обёртка для сценариев

Добавить над `ask_perplexity` или рядом с ним лёгкую обёртку, например:

```python
async def call_llm_scenario(scenario_name: str, messages: list[dict[str, str]], **kwargs) -> str:
    # 1. Логируем сценарий, промпт, параметры
    # 2. Вызываем ask_perplexity(...)
    # 3. Логируем сырое содержимое ответа
    # 4. Возвращаем текст
```

И использовать её в:

- `analyze_lot` (scenario_name="analyze_lot")
- `analyze_documentation` (scenario_name="analyze_documentation")
- `search_suppliers_perplexity` (scenario_name="search_suppliers")

Так на защите можно будет сказать: "В коде есть пять сценариев вызова LLM, большинство обёрнуты общей функцией `call_llm_scenario()`, все вызовы прозрачно логируются".

***

### 5. Демонстрация в Telegram‑боте

#### 5.1. Наглядный вывод результатов

В текущей логике:

- `analyze_lot` возвращает текст анализа, который бот отправляет пользователю одним сообщением.[^1]
- `analyze_documentation` — аналогично.[^1]
- `search_suppliers_perplexity` — формирует текст со списком поставщиков.[^1]

Требуется:

1. **В коде бота** (вне `perplexity.py`, но в рамках ТЗ можно описать для доработки):
    - Дополнительно показывать пользователю (администратору/разработчику) **служебный блок** по запросу (например, по команде `/debug_lot`):
        - Краткий summary: какой сценарий вызван.
        - Ссылка или id лога (если используете Loguru с file-handler).
        - (опционально) урезанный фрагмент промпта и ответа.
2. Для защиты проекта достаточно, чтобы **по логам** можно было:
    - Показать промпт и ответ для конкретного `lot_number`.
    - Объяснить, какие поля лота использованы (один‑два скриншота кода + лога).

***

### 6. Изменения в коде (конкретные задачи для Cursor)

Сформулируем задачи, которые можно реализовывать по шагам.

#### Задача 1. Вынести текстовые инструкции промптов в константы

- Создать в `perplexity.py` (или отдельном модуле `prompts.py`) константы:
    - `ENHANCED_ANALYSIS_SYSTEM_PROMPT` — текст system‑сообщения для `analyze_lot(enhanced=True)`.![^1]
    - `ENHANCED_ANALYSIS_USER_TEMPLATE` — шаблон user‑промпта, который заполняется данными `Lot` и бюджетными настройками.[^1]
    - Аналогичный набор для `analyze_documentation`.![^1]
    - `SUPPLIERS_SEARCH_SYSTEM_PROMPT` и `SUPPLIERS_SEARCH_USER_TEMPLATE` для `search_suppliers_perplexity`.![^1]
- В константах чётко выделить блок `<АЛГОРИТМ АНАЛИЗА>`.


#### Задача 2. Добавить функцию `call_llm_scenario`

- Реализовать обёртку:

```python
async def call_llm_scenario(
    scenario_name: str,
    messages: list[dict[str, str]],
    model: str | None = None,
    temperature: float = 0.2,
    max_tokens: int | None = 900,
) -> str:
    # Логирование входных данных (scenario_name, длина промпта)
    # Логирование system/user-сообщений (с обрезкой по длине)
    # Вызов ask_perplexity(...)
    # Логирование первых N символов ответа
    # Возврат текста
```

- В `analyze_lot`, `analyze_documentation`, `search_suppliers_perplexity` заменить прямой вызов `ask_perplexity` на `call_llm_scenario` с соответствующим `scenario_name`.


#### Задача 3. Добавить подробное логирование источников данных

В каждой из функций:

1. `analyze_lot`:
    - Перед формированием промпта залогировать:
        - `lot.id`, `lot.lot_number`, `lot.platform_name`, `lot.customer`, `lot.budget`, `lot.deadline`, `lot.status`.![^1]
        - Размер `nomenclature` и первые N элементов.![^1]
        - `budget_min`, `budget_max`, `overhead_pct`.![^1]
2. `analyze_documentation`:
    - Логировать:
        - Связанный `lot` (как выше).![^1]
        - Длину `documentation_text` и первые, например, 500 символов.![^1]
3. `search_suppliers_perplexity`:
    - Логировать: `product_name`, `max_suppliers`.![^1]

Это даст готовые ответы на вопрос №2 куратора («Источник информации»).

#### Задача 4. Уточнить формат ответов для наглядности в боте

- Убедиться, что формат, описанный в промптах (`📊 ОЦЕНКИ ПО ПАРАМЕТРАМ`, `🎯 ИНТЕГРАЛЬНАЯ ОЦЕНКА`, `✅ РЕКОМЕНДАЦИЯ`, `📋 РИСКИ`, `🚀 ШАГИ`), соблюдается и хорошо читается в Telegram.[^1]
- При необходимости слегка подправить эмодзи/разделители, чтобы сообщение не выглядело перегруженным.

***

### 7. Ответы на ключевые вопросы куратора (как результат ТЗ)

После выполнения ТЗ вы сможете на защите:

1. **Сколько раз в алгоритме реализовано обращение к LLM?**
    - Показать пять главных точек:
        - `analyze_lot` (анализ лота).
        - `analyze_documentation` (анализ документации).
        - `search_suppliers_perplexity` (поиск поставщиков).
        - `analyze_supplier_reliability` (анализ надежности поставщика).
        - `check_nomenclature_match_with_llm` (семантическое сопоставление номенклатуры).![^1]
2. **Какая информация передаётся модели и откуда?**
    - Показать фрагменты кода и логов, где видно:
        - поля объекта `Lot`;
        - текст документации;
        - строка `product_name`.![^1]
3. **Показать промпт для каждой модели.**
    - Показать константы `*_SYSTEM_PROMPT` и `*_USER_TEMPLATE` + сформированный реальный промпт из логов.[^1]
4. **Показать результат генерации в Telegram‑боте.**
    - Открыть переписку с ботом, где видно:
        - сообщение с анализом лота;
        - сообщение с анализом документации;
        - сообщение со списком поставщиков.![^1]

***

Если нужно, могу отдельно подготовить шаблон конкретных констант‑промптов в коде (с уже вписанным блоком `<АЛГОРИТМ АНАЛИЗА>`), чтобы ты просто вставил их в Cursor.

<div align="center">⁂</div>

[^1]: perplexity.py

