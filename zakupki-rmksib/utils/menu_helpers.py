"""Утилиты для работы с кнопками меню"""
from typing import List


def get_menu_buttons() -> List[str]:
    """Возвращает список всех кнопок главного меню"""
    return [
        "📋 Мои лоты",
        "📊 Статистика",
        "📄 Анализ КП",
        "🔍 Поиск Поставщиков",
        "➕ Создать лот",
        "⚙️ Настройки",
        "👑 Админ-панель"
    ]


def is_menu_button(text: str) -> bool:
    """Проверяет, является ли текст кнопкой меню"""
    return text in get_menu_buttons()


async def handle_menu_button_in_fsm(message, state, button_text: str) -> bool:
    """
    Обрабатывает нажатие кнопки меню в FSM состоянии
    
    Args:
        message: Сообщение от пользователя
        state: FSM контекст
        button_text: Текст кнопки
    
    Returns:
        True если это кнопка меню и состояние очищено, False если нет
    """
    if is_menu_button(button_text):
        await state.clear()
        return True
    return False

