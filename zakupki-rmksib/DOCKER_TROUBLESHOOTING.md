# Решение проблем с Docker

## ❌ Проблема: Docker Desktop не запущен

### Симптомы:
```
error during connect: Get "http://%2F%2F.%2Fpipe%2FdockerDesktopLinuxEngine/v1.51/...": 
open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.
```

### Решение:

#### Вариант 1: Запустить Docker Desktop
1. Откройте **Docker Desktop** на Windows
2. Дождитесь полного запуска (иконка в трее должна быть зеленая)
3. Попробуйте снова:
```bash
docker compose up -d
```

#### Вариант 2: Локальный запуск без Docker (рекомендуется для разработки)

Если Docker Desktop не установлен или не работает, используйте локальный запуск:

**Для админ-панели:**
```bash
# 1. Убедитесь, что PostgreSQL запущен локально
# Или используйте существующую БД

# 2. Перейдите в директорию админ-панели
cd admin_panel

# 3. Установите зависимости (если еще не установлены)
pip install -r ../requirements.txt

# 4. Примените миграции
python manage.py migrate --run-syncdb

# 5. Создайте суперпользователя
python scripts/create_django_admin.py

# 6. Запустите сервер
python manage.py runserver

# 7. Откройте в браузере
# http://localhost:8000/admin/
```

**Для бота:**
```bash
# 1. Убедитесь, что PostgreSQL и Redis запущены локально

# 2. Установите зависимости
pip install -r requirements.txt

# 3. Примените миграции Alembic
alembic upgrade head

# 4. Запустите бота
python -m bot.main
```

## ⚠️ Предупреждение: Устаревший атрибут `version`

### Симптомы:
```
level=warning msg="the attribute `version` is obsolete, it will be ignored"
```

### Решение:
Атрибут `version` уже удален из `docker-compose.yml`. Это предупреждение можно игнорировать, оно не влияет на работу.

Если вы видите это предупреждение в других проектах, просто удалите строку `version: "3.9"` из начала файла `docker-compose.yml`.

## 🔍 Проверка статуса Docker

### Проверка, запущен ли Docker:
```bash
docker ps
```

Если команда выполняется без ошибок, Docker работает.

### Проверка версии Docker Compose:
```bash
docker compose version
```

## 📋 Альтернативные варианты запуска

### Вариант 1: Использовать существующую БД PostgreSQL

Если у вас уже есть PostgreSQL, запущенный локально или удаленно:

1. **Настройте `.env` файл:**
```env
POSTGRES_HOST=localhost  # или IP адрес вашего сервера
POSTGRES_PORT=5432
POSTGRES_DB=procurement
POSTGRES_USER=your_user
POSTGRES_PASSWORD=your_password
```

2. **Запустите админ-панель локально:**
```bash
cd admin_panel
python manage.py migrate --run-syncdb
python scripts/create_django_admin.py
python manage.py runserver
```

### Вариант 2: Использовать SQLite для разработки (не рекомендуется для продакшена)

Если нужно быстро протестировать админ-панель без PostgreSQL:

1. **Измените `admin_panel/admin_panel/settings.py`:**
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```

**⚠️ Внимание:** Это только для тестирования! SQLite не поддерживает все функции PostgreSQL (например, JSONField может работать по-другому).

## 🛠️ Установка Docker Desktop (если не установлен)

1. Скачайте Docker Desktop для Windows: https://www.docker.com/products/docker-desktop/
2. Установите и запустите Docker Desktop
3. Дождитесь полного запуска
4. Попробуйте снова запустить `docker compose up -d`

## 📞 Дополнительная помощь

Если проблемы сохраняются:

1. **Проверьте логи Docker:**
```bash
docker compose logs
```

2. **Перезапустите Docker Desktop:**
   - Закройте Docker Desktop
   - Откройте снова
   - Дождитесь полного запуска

3. **Проверьте настройки Docker:**
   - Откройте Docker Desktop → Settings
   - Убедитесь, что Docker Engine запущен

4. **Используйте локальный запуск** (см. выше) - это проще для разработки

---

**Рекомендация:** Для разработки проще использовать локальный запуск без Docker. Docker полезен для продакшена и для обеспечения одинакового окружения для всех разработчиков.

