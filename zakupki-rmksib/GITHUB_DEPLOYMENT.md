# Подробная инструкция: Размещение проекта на GitHub

## Содержание
1. [Подготовка проекта](#подготовка-проекта)
2. [Создание репозитория на GitHub](#создание-репозитория-на-github)
3. [Настройка Git в проекте](#настройка-git-в-проекте)
4. [Первый коммит и отправка](#первый-коммит-и-отправка)
5. [Дополнительные настройки](#дополнительные-настройки)
6. [Частые проблемы и решения](#частые-проблемы-и-решения)

---

## Подготовка проекта

### Шаг 1: Проверка и обновление .gitignore

Убедитесь, что файл `.gitignore` содержит все необходимые исключения. Для вашего проекта рекомендуется добавить:

```gitignore
# Существующие правила
.env
__pycache__/ 
*.pyc
*.pyo
*.pyd
.Python
.env.*
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.DS_Store
.idea/
.vscode/

# Дополнительные правила для вашего проекта
*.db
*.sqlite
*.sqlite3
procurement.db
data/documentation/**/documents/
*.log
*.bak
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Файлы с конфиденциальной информацией
SECURITY_ENCRYPTION_KEY.md
*.key
*.pem
*.cert

# Временные файлы
*.tmp
*.temp
*.zip
```

### Шаг 2: Проверка конфиденциальных данных

**ВАЖНО!** Перед публикацией убедитесь, что в репозитории нет:
- API ключей и токенов (`.env` файлы)
- Паролей и секретных ключей
- Персональных данных пользователей
- Файлов базы данных с реальными данными

Проверьте файлы:
- `config/settings.py` — нет ли хардкодных ключей
- `SECURITY_ENCRYPTION_KEY.md` — должен быть в `.gitignore`
- Все `.env` файлы — должны быть исключены

### Шаг 3: Создание .env.example (если его нет)

Создайте файл `.env.example` с примером переменных окружения (без реальных значений):

```env
# Telegram Bot Token
BOT_TOKEN=your_bot_token_here

# Database
DATABASE_URL=sqlite:///procurement.db

# Email settings
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your_email@example.com
EMAIL_PASSWORD=your_password_here

# API Keys
PERPLEXITY_API_KEY=your_perplexity_api_key_here
SNIPER_SEARCH_API_KEY=your_sniper_search_api_key_here
```

---

## Создание репозитория на GitHub

### Шаг 1: Вход в GitHub

1. Откройте браузер и перейдите на [github.com](https://github.com)
2. Войдите в свой аккаунт (или создайте новый)

### Шаг 2: Создание нового репозитория

1. Нажмите на кнопку **"+"** в правом верхнем углу
2. Выберите **"New repository"**
3. Заполните форму:
   - **Repository name**: `zakupki-rmksib` (или другое имя)
   - **Description**: "AI-ассистент закупок для Telegram бота"
   - **Visibility**: 
     - **Public** — если хотите открытый репозиторий
     - **Private** — если проект приватный (рекомендуется для коммерческих проектов)
   - **НЕ** ставьте галочки на:
     - ❌ Add a README file (у вас уже есть)
     - ❌ Add .gitignore (у вас уже есть)
     - ❌ Choose a license (можно добавить позже)
4. Нажмите **"Create repository"**

### Шаг 3: Копирование URL репозитория

После создания репозитория GitHub покажет страницу с инструкциями. Скопируйте **HTTPS URL** репозитория, например:
```
https://github.com/ваш-username/zakupki-rmksib.git
```

---

## Настройка Git в проекте

### Шаг 1: Проверка текущего состояния

Откройте терминал в корне проекта и выполните:

```bash
# Проверка статуса
git status

# Проверка текущей ветки
git branch
```

### Шаг 2: Настройка Git (если еще не настроено)

```bash
# Установка имени пользователя (если еще не установлено)
git config --global user.name "Ваше Имя"

# Установка email (должен совпадать с email на GitHub)
git config --global user.email "your.email@example.com"

# Проверка настроек
git config --list
```

### Шаг 3: Добавление удаленного репозитория

```bash
# Добавление удаленного репозитория (замените URL на ваш)
git remote add origin https://github.com/ваш-username/zakupki-rmksib.git

# Проверка удаленных репозиториев
git remote -v
```

Если репозиторий уже был добавлен ранее, но с другим URL:
```bash
# Изменение URL удаленного репозитория
git remote set-url origin https://github.com/ваш-username/zakupki-rmksib.git
```

---

## Первый коммит и отправка

### Шаг 1: Добавление файлов в staging area

```bash
# Добавление всех файлов (кроме тех, что в .gitignore)
git add .

# Проверка, какие файлы будут добавлены
git status
```

**Проверьте**, что в список не попали:
- `.env` файлы
- `__pycache__/` директории
- `procurement.db` (база данных)
- Файлы с секретами

### Шаг 2: Создание первого коммита

```bash
# Создание коммита с описанием
git commit -m "Initial commit: AI-ассистент закупок для Telegram бота"

# Или более подробное описание:
git commit -m "Initial commit

- Telegram бот для управления закупками
- AI-анализ лотов через Perplexity API
- Система управления поставщиками и коммерческими предложениями
- Email уведомления
- База данных с миграциями Alembic"
```

### Шаг 3: Переименование ветки (опционально)

GitHub теперь использует `main` вместо `master` по умолчанию:

```bash
# Переименование текущей ветки в main
git branch -M main
```

### Шаг 4: Отправка кода на GitHub

```bash
# Отправка кода в удаленный репозиторий
git push -u origin main

# Если ваша ветка называется master:
git push -u origin master
```

**Примечание**: При первом push Git может запросить авторизацию:
- **HTTPS**: Потребуется токен доступа (Personal Access Token)
- **SSH**: Нужно настроить SSH ключи

### Шаг 5: Авторизация (если требуется)

#### Вариант A: Использование Personal Access Token (HTTPS)

1. Перейдите в GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите **"Generate new token"**
3. Выберите права доступа: `repo` (полный доступ к репозиториям)
4. Скопируйте токен (он показывается только один раз!)
5. При запросе пароля в Git используйте этот токен вместо пароля

#### Вариант B: Использование SSH (рекомендуется)

1. Генерация SSH ключа:
```bash
ssh-keygen -t ed25519 -C "your.email@example.com"
# Нажмите Enter для всех вопросов
```

2. Копирование публичного ключа:
```bash
# Windows (Git Bash)
cat ~/.ssh/id_ed25519.pub

# Скопируйте вывод команды
```

3. Добавление ключа в GitHub:
   - GitHub → Settings → SSH and GPG keys → New SSH key
   - Вставьте скопированный ключ
   - Сохраните

4. Изменение URL репозитория на SSH:
```bash
git remote set-url origin git@github.com:ваш-username/zakupki-rmksib.git
```

5. Проверка подключения:
```bash
ssh -T git@github.com
```

---

## Дополнительные настройки

### Шаг 1: Обновление README.md

Убедитесь, что `README.md` содержит:
- Описание проекта
- Инструкции по установке
- Требования
- Примеры использования
- Информацию о лицензии (если есть)

### Шаг 2: Добавление лицензии (опционально)

Если хотите добавить лицензию:

```bash
# Создание файла LICENSE (например, MIT)
# Или используйте веб-интерфейс GitHub: Add file → Create new file → LICENSE
```

### Шаг 3: Настройка репозитория на GitHub

1. Перейдите в **Settings** репозитория
2. Настройте:
   - **Topics** — добавьте теги: `python`, `telegram-bot`, `ai`, `procurement`
   - **Description** — краткое описание
   - **Website** — если есть сайт проекта
   - **Features** — включите Issues, Wiki (если нужно)

### Шаг 4: Создание ветки разработки (опционально)

```bash
# Создание ветки для разработки
git checkout -b develop

# Отправка ветки на GitHub
git push -u origin develop
```

---

## Частые проблемы и решения

### Проблема 1: "Permission denied" при push

**Решение**:
- Проверьте правильность URL репозитория
- Убедитесь, что используете правильный токен доступа (для HTTPS)
- Проверьте SSH ключи (для SSH)

### Проблема 2: Git пытается отслеживать файлы из родительской директории

**Решение**:
```bash
# Проверьте, где находится .git директория
ls -la .git

# Если .git находится не в корне проекта, удалите его и создайте заново
rm -rf .git
git init
```

### Проблема 3: Конфликт при push (если репозиторий не пустой)

**Решение**:
```bash
# Получение изменений с GitHub
git pull origin main --allow-unrelated-histories

# Разрешение конфликтов (если есть)
# Затем:
git add .
git commit -m "Merge remote-tracking branch"
git push origin main
```

### Проблема 4: Забыли добавить файл в .gitignore

**Решение**:
```bash
# Удаление файла из индекса Git (но не с диска)
git rm --cached файл_который_не_нужен

# Добавление в .gitignore
echo "файл_который_не_нужен" >> .gitignore

# Коммит изменений
git add .gitignore
git commit -m "Add file to .gitignore"
git push origin main
```

### Проблема 5: Нужно обновить .gitignore для базы данных

**Решение**:
```bash
# Добавьте в .gitignore
echo "*.db" >> .gitignore
echo "*.sqlite" >> .gitignore
echo "procurement.db" >> .gitignore

# Удалите базу данных из индекса (если уже добавлена)
git rm --cached procurement.db

# Коммит
git add .gitignore
git commit -m "Exclude database files from git"
git push origin main
```

---

## Последующие обновления проекта

После первого размещения проекта, для обновления кода используйте:

```bash
# Проверка изменений
git status

# Добавление изменений
git add .

# Создание коммита
git commit -m "Описание изменений"

# Отправка на GitHub
git push origin main
```

### Рекомендации по коммитам

Используйте понятные сообщения коммитов:
- `feat: добавлена функция поиска поставщиков`
- `fix: исправлена ошибка парсинга email`
- `docs: обновлена документация`
- `refactor: рефакторинг модуля парсинга`

---

## Проверка результата

После успешного push:

1. Откройте ваш репозиторий на GitHub
2. Убедитесь, что все файлы загружены
3. Проверьте, что `.env` и другие секретные файлы **НЕ** видны
4. Проверьте, что `README.md` отображается корректно

---

## Полезные команды Git

```bash
# Просмотр истории коммитов
git log --oneline

# Просмотр изменений
git diff

# Просмотр удаленных репозиториев
git remote -v

# Изменение последнего коммита (если еще не отправили)
git commit --amend

# Отмена изменений в файле (до git add)
git checkout -- файл

# Отмена изменений в staging area
git reset HEAD файл
```

---

## Безопасность

**Перед публикацией обязательно проверьте:**

1. ✅ Нет ли API ключей в коде
2. ✅ `.env` файлы в `.gitignore`
3. ✅ База данных не загружена
4. ✅ Нет паролей и секретов в коммитах
5. ✅ Файлы с конфиденциальной информацией исключены

**Если случайно загрузили секреты:**

1. Немедленно измените все ключи/пароли
2. Используйте `git filter-branch` или `BFG Repo-Cleaner` для удаления из истории
3. Или создайте новый репозиторий

---

## Дополнительные ресурсы

- [Официальная документация Git](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

---

**Успешной публикации проекта! 🚀**

