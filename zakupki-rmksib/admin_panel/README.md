# Django Admin Panel для проекта "Закупки РМКСИБ"

Веб-админ-панель на Django для управления данными системы закупок.

## 🚀 Быстрый старт

### Локальный запуск (без Docker)

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

2. **Убедитесь, что PostgreSQL запущен и доступен:**
```bash
# Проверьте настройки в .env файле
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=procurement
POSTGRES_USER=procure_user
POSTGRES_PASSWORD=change_me
```

3. **Перейдите в директорию админ-панели:**
```bash
cd admin_panel
```

4. **Примените миграции:**
```bash
python manage.py migrate --run-syncdb
```

5. **Создайте суперпользователя:**
```bash
python scripts/create_django_admin.py
```

Или используйте стандартную команду Django:
```bash
python manage.py createsuperuser
```

6. **Соберите статические файлы:**
```bash
python manage.py collectstatic --noinput
```

7. **Запустите сервер разработки:**
```bash
python manage.py runserver
```

8. **Откройте в браузере:**
```
http://localhost:8000/admin/
```

### Запуск через Docker Compose

Админ-панель автоматически запускается вместе с основным проектом:

```bash
docker compose up -d admin_panel
```

Админ-панель будет доступна по адресу: `http://localhost:8000/admin/`

**Важно:** После первого запуска создайте суперпользователя:
```bash
docker compose exec admin_panel python scripts/create_django_admin.py
```

## 📋 Функциональность

### Управление пользователями
- Просмотр всех пользователей системы
- Фильтрация по ролям (user, manager, admin)
- Поиск по имени, username, email, Telegram ID
- Редактирование ролей и статусов

### Управление лотами
- Просмотр всех лотов с фильтрацией
- Фильтры по статусу, платформе, дате создания
- Поиск по номеру лота, названию, описанию
- Визуальные индикаторы статусов (цветные бейджи)
- Отображение просроченных лотов
- Связь с владельцами (пользователями)

### Управление поставщиками
- Просмотр базы поставщиков
- Фильтрация по рейтингу надежности
- Поиск по названию, ИНН, email, телефону
- Отображение рейтинга с цветовыми индикаторами

### Управление коммерческими предложениями
- Просмотр всех КП
- Фильтрация по дате создания, анализу, рейтингу
- Поиск по названию поставщика, ИНН, тексту КП
- Отображение финансовых данных (цена, доставка, итого)
- Интегральный рейтинг с визуальными индикаторами
- Связь с лотами и пользователями

### Управление настройками пользователей
- Просмотр и редактирование настроек уведомлений
- Управление фильтрами (заказчики, номенклатура)
- Настройки бюджета
- Email настройки

## 🔧 Настройка

### Переменные окружения

Добавьте в `.env` файл (если еще не добавлено):

```env
# Django настройки
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# База данных (используется та же, что и для бота)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=procurement
POSTGRES_USER=procure_user
POSTGRES_PASSWORD=change_me
```

### Безопасность

Для продакшена обязательно:
1. Измените `DJANGO_SECRET_KEY` на случайную строку
2. Установите `DJANGO_DEBUG=False`
3. Настройте `DJANGO_ALLOWED_HOSTS` с вашим доменом
4. Используйте HTTPS
5. Настройте правильные права доступа к файлам

## 📊 Особенности

### Использование существующей базы данных

Админ-панель использует ту же базу данных PostgreSQL, что и основной Telegram бот. Django модели соответствуют SQLAlchemy моделям из `database/models.py`.

**Важно:** Django не создает новые таблицы, а использует существующие. Миграции Django применяются только для служебных таблиц Django (auth, sessions и т.д.).

### Синхронизация данных

Данные автоматически синхронизируются между ботом и админ-панелью, так как они используют одну базу данных.

## 🛠️ Разработка

### Структура проекта

```
admin_panel/
├── admin_panel/          # Настройки Django проекта
│   ├── settings.py       # Конфигурация
│   ├── urls.py           # URL маршруты
│   └── wsgi.py           # WSGI приложение
├── procurement/          # Django приложение
│   ├── models.py         # Модели данных
│   ├── admin.py          # Конфигурация админ-панели
│   └── apps.py           # Конфигурация приложения
├── scripts/              # Вспомогательные скрипты
│   └── create_django_admin.py
├── manage.py             # Django management script
└── README.md             # Этот файл
```

### Добавление новых функций

1. **Новые модели:** Добавьте в `procurement/models.py`
2. **Админ-панель:** Настройте в `procurement/admin.py`
3. **Миграции:** `python manage.py makemigrations` и `python manage.py migrate`

## 📝 Команды управления

```bash
# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser

# Собрать статические файлы
python manage.py collectstatic

# Запустить сервер разработки
python manage.py runserver

# Создать миграции для новых моделей
python manage.py makemigrations

# Открыть Django shell
python manage.py shell
```

## 🔗 Ссылки

- **Админ-панель:** http://localhost:8000/admin/
- **Документация Django:** https://docs.djangoproject.com/
- **Документация проекта:** См. README.md в корне проекта

## ⚠️ Важные замечания

1. **Не создавайте миграции для существующих таблиц** - они уже созданы через Alembic
2. **Используйте `--run-syncdb`** при первой настройке для создания служебных таблиц Django
3. **Не изменяйте структуру таблиц через Django** - используйте Alembic миграции из основного проекта
4. **Резервное копирование** - делайте бэкапы БД перед изменениями

## 🐛 Решение проблем

### Ошибка подключения к БД
- Проверьте настройки в `.env`
- Убедитесь, что PostgreSQL запущен
- Проверьте права доступа пользователя БД

### Ошибка миграций
- Используйте `--run-syncdb` для первой настройки
- Не создавайте миграции для существующих таблиц

### Статические файлы не загружаются
- Выполните `python manage.py collectstatic`
- Проверьте настройки `STATIC_ROOT` и `STATIC_URL` в `settings.py`

---

**Разработано для проекта "Закупки РМКСИБ — AI-ассистент закупок"**

