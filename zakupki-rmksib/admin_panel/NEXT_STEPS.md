# Следующие шаги после создания пользователя PostgreSQL

## ✅ Что уже сделано

1. ✅ Пользователь `procure_user` создан
2. ✅ База данных `procurement` создана
3. ✅ Права выданы

## 📋 Следующие шаги

### Шаг 1: Выйдите из psql

В psql выполните:
```
\q
```

Это выйдет из psql и вернет вас в обычный терминал.

### Шаг 2: Проверьте подключение

После выхода из psql выполните:

```bash
python check_db_connection.py
```

Вы должны увидеть:
```
✅ Подключение успешно!
Версия PostgreSQL: ...
```

### Шаг 3: Верните настройки PostgreSQL в Django

Если вы использовали SQLite, нужно вернуть настройки PostgreSQL в `admin_panel/admin_panel/settings.py`.

Убедитесь, что в файле `settings.py` секция `DATABASES` выглядит так:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.getenv('POSTGRES_DB', 'procurement'),
        'USER': os.getenv('POSTGRES_USER', 'procure_user'),
        'PASSWORD': os.getenv('POSTGRES_PASSWORD', 'change_me'),
        'HOST': os.getenv('POSTGRES_HOST', 'localhost'),
        'PORT': os.getenv('POSTGRES_PORT', '5432'),
        'OPTIONS': {
            'options': '-c search_path=public -c client_encoding=UTF8'
        },
        'CONN_MAX_AGE': 600,
    }
}
```

### Шаг 4: Примените миграции Django

```bash
python manage.py migrate --run-syncdb
```

Это создаст необходимые таблицы Django в PostgreSQL.

### Шаг 5: Создайте суперпользователя Django

```bash
python scripts/create_django_admin.py
```

Или используйте стандартную команду:
```bash
python manage.py createsuperuser
```

### Шаг 6: Запустите сервер

```bash
python manage.py runserver
```

### Шаг 7: Откройте админ-панель

Откройте в браузере:
```
http://localhost:8000/admin/
```

Войдите с созданными учетными данными.

## ⚠️ Важно

Если вы использовали SQLite ранее, теперь данные будут в PostgreSQL. Это правильная настройка для продакшена.

## 🐛 Если возникнут проблемы

1. **Ошибка подключения**: Проверьте, что PostgreSQL запущен и пользователь создан правильно
2. **Ошибка миграций**: Убедитесь, что база данных пустая или используйте `--run-syncdb`
3. **Ошибка кодировки**: Убедитесь, что в `settings.py` указана кодировка UTF8 в OPTIONS

