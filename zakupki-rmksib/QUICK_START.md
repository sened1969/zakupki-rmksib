# 🚀 Быстрый запуск бота

## Шаг 1: Подготовка окружения

### Установите зависимости:
```bash
pip install -r requirements.txt
```

### Создайте файл .env:
Создайте файл `.env` в корне проекта (скопируйте из `.env.example`):

**Windows PowerShell:**
```powershell
Copy-Item .env.example .env
```

**Linux/Mac:**
```bash
cp .env.example .env
```

**Или создайте вручную** файл `.env` с содержимым:

```env
BOT_TOKEN=your_bot_token_here
REDIS_URL=redis://localhost:6379/0
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=procurement
POSTGRES_USER=procure_user
POSTGRES_PASSWORD=change_me
PERPLEXITY_API_KEY=your_perplexity_api_key_here
NOTIFY_EMAILS=["sp@rmksib.ru", "bo@rmksib.ru", "info@rmksib.ru", "sened17@yandex.ru"]
```

**Для email уведомлений** (опционально) добавьте:
```env
SMTP_HOST=smtp.your-domain.com
SMTP_USER=your-email@domain.com
SMTP_PASS=your-password
COMPANY_EMAIL=noreply@rmksib.ru
```

**Для безопасности** (рекомендуется для продакшена):
```env
# Сгенерируйте ключ: python scripts/generate_encryption_key.py
ENCRYPTION_KEY=your_generated_key_here
```

⚠️ **Важно:** Без `ENCRYPTION_KEY` будет использоваться небезопасный ключ по умолчанию.  
См. `SECURITY_ENCRYPTION_KEY.md` для подробностей.

## Шаг 2: Запустите Docker

```bash
docker compose up -d
```

Это запустит PostgreSQL и Redis в фоновом режиме.

## Шаг 3: Примените миграции БД

```bash
alembic upgrade head
```

## Шаг 4: Создайте администратора

### Как узнать свой Telegram ID?

**Способ 1: Через бота @userinfobot (рекомендуется)**
1. Откройте Telegram
2. Найдите бота `@userinfobot`
3. Нажмите `/start`
4. Бот покажет ваш **Telegram ID** (например: `6208324414`)

**Способ 2: Через ваш бот (после запуска)**
1. Запустите бота (см. Шаг 5)
2. Напишите боту `/user_info`
3. В ответе будет ваш **Telegram ID**

### Создание администратора:

**Используйте ваш Telegram ID: `6208324414`**

**Команда для создания администратора:**
```bash
python scripts/create_admin.py 6208324414
```

**Что произойдет:**
- Создастся пользователь с ролью `admin`
- Вы получите полный доступ ко всем командам бота

## Шаг 5: Запустите бота

```bash
python -m bot.main
```

Бот должен запуститься и начать отвечать на сообщения!

## Проверка работы

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Должно появиться приветствие и меню

## Остановка бота

Нажмите `Ctrl + C` в терминале

---

## 🐛 Если что-то не работает:

### Ошибка подключения к БД:
```bash
docker compose down
docker compose up -d
alembic upgrade head
```

### Порт занят:
Измените порты в `docker-compose.yml` или `.env`

### Токен не работает:
Получите новый токен у @BotFather в Telegram

---

## Полезные команды:

```bash
# Посмотреть логи Docker
docker compose logs -f bot

# Остановить Docker
docker compose down

# Перезапустить Docker
docker compose restart

# Посмотреть статус
docker compose ps
```

