# Руководство по безопасности проекта

## 🔒 Критические правила безопасности

### ❌ НИКОГДА не делайте:

1. **НЕ коммитьте реальные API ключи** в репозиторий
2. **НЕ добавляйте `.env` файл** в Git (уже в `.gitignore`)
3. **НЕ добавляйте реальные ключи** в документацию
4. **НЕ публикуйте скриншоты** с реальными ключами
5. **НЕ делитесь ключами** через незащищенные каналы

### ✅ ВСЕГДА делайте:

1. **Используйте `.env` файл** для хранения всех секретов
2. **Используйте `.env.example`** с плейсхолдерами для документации
3. **Проверяйте файлы перед коммитом** на наличие секретов
4. **Используйте плейсхолдеры** в документации (`your_bot_token_here`)
5. **Регулярно ротируйте ключи** (меняйте периодически)

## 📋 Текущие ключи (хранятся только в .env)

**ВНИМАНИЕ:** Реальные ключи хранятся **ТОЛЬКО** в файле `.env`, который **НЕ** коммитится в Git.

### Структура .env файла:

```env
# Telegram Bot Token
BOT_TOKEN=ваш_токен_здесь

# Perplexity API Key
PERPLEXITY_API_KEY=ваш_ключ_здесь

# Другие секреты...
```

## 🔍 Проверка перед коммитом

### Автоматическая проверка

Перед каждым коммитом выполните:

```bash
# Проверка на наличие реальных ключей Perplexity
grep -r "pplx-[A-Za-z0-9]\{40,\}" . --exclude-dir=.git --exclude="*.md"

# Проверка на наличие Telegram токенов
grep -r "[0-9]\{10\}:[A-Za-z0-9_-]\{35\}" . --exclude-dir=.git --exclude="*.md"

# Проверка на наличие .env файлов
find . -name ".env*" -not -path "./.git/*"
```

### Ручная проверка

Перед коммитом проверьте:
1. Нет ли реальных ключей в файлах документации
2. Все ли ключи заменены на плейсхолдеры
3. Не попал ли `.env` файл в staging area

```bash
git status
git diff --cached
```

## 🛡️ Защита .gitignore

Файл `.gitignore` настроен для защиты:

- ✅ `.env` и все варианты `.env.*`
- ✅ Файлы с расширениями `.key`, `.pem`, `.cert`
- ✅ Файлы содержащие `secret`, `SECRET`, `PASSWORD`, `TOKEN`
- ✅ Документация с секретами

## 🚨 Если ключи все же попали в репозиторий

### Немедленные действия:

1. **Отзовите ключи** в соответствующих сервисах
2. **Создайте новые ключи**
3. **Обновите `.env` файл**
4. **Удалите ключи из файлов** в репозитории
5. **Создайте новый коммит** с исправлениями

### Удаление из истории Git (только для приватных репозиториев):

```bash
# Использование BFG Repo-Cleaner (рекомендуется)
# Или git filter-branch
```

**⚠️ ВАЖНО:** Если репозиторий публичный, ключи уже могли быть скопированы. Обязательно смените их!

## 📝 Примеры правильной документации

### ✅ ПРАВИЛЬНО:

```env
BOT_TOKEN=your_bot_token_here
PERPLEXITY_API_KEY=your_perplexity_api_key_here
```

### ❌ НЕПРАВИЛЬНО:

```env
# НИКОГДА не добавляйте реальные ключи в документацию!
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz1234567890
PERPLEXITY_API_KEY=pplx-ABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890abcdefghij
```

**Примечание:** Даже в примерах "неправильного" использования не указывайте реальные ключи!

## 🔧 Настройка git-secrets (рекомендуется)

Для автоматической проверки секретов:

```bash
# Установка git-secrets
git clone https://github.com/awslabs/git-secrets.git
cd git-secrets
sudo make install

# Настройка для проекта
git secrets --install
git secrets --register-aws

# Добавление паттернов для проверки
git secrets --add 'pplx-[A-Za-z0-9]+'
git secrets --add '[0-9]{10}:[A-Za-z0-9_-]{35}'
git secrets --add 'BOT_TOKEN=.*[^here]$'
```

## 📞 Контакты при проблемах безопасности

При обнаружении утечки секретов:
1. Немедленно отзовите все ключи
2. Проверьте логи использования
3. Создайте новые ключи
4. Обновите `.env` файл

---

**Последнее обновление:** 25 ноября 2025  
**Статус:** Активная защита настроена

