# Руководство для куратора — Тестирование бота Закупки РМКСИБ

## 📋 Быстрый старт для куратора

### 1. Получение доступа к боту

#### Шаг 1: Получите токен бота
Попросите у разработчика:
- `BOT_TOKEN` — токен вашего Telegram бота
- Или готовый `.env` файл с настройками

#### Шаг 2: Создайте .env файл
Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=your_bot_token_here
REDIS_URL=redis://localhost:6379/0
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=procurement
POSTGRES_USER=procure_user
POSTGRES_PASSWORD=change_me
PERPLEXITY_API_KEY=your_perplexity_api_key_here
```

#### Шаг 3: Установите зависимости
```bash
pip install -r requirements.txt
```

#### Шаг 4: Запустите Docker
```bash
docker compose up -d
```

Это запустит PostgreSQL и Redis.

#### Шаг 5: Примените миграции
```bash
alembic upgrade head
```

#### Шаг 6: Создайте первого администратора
```bash
python scripts/create_admin.py YOUR_TELEGRAM_ID
```

Замените `YOUR_TELEGRAM_ID` на ваш Telegram ID (можно узнать у бота @userinfobot).

#### Шаг 7: Запустите бота
```bash
python -m bot.main
```

---

## 🧪 Сценарий тестирования

### Тест 1: Базовая функциональность

1. **Запуск бота**
   - Бот должен ответить на `/start`
   - Должно появиться главное меню (4 кнопки)

2. **Регистрация**
   - При первом входе пользователь автоматически регистрируется
   - Проверьте `/user_info` — должны отображаться ваши данные

3. **Главное меню**
   - Нажмите каждую кнопку и проверьте ответы:
     - 📋 Мои лоты
     - 🚛 Поставщики
     - 📊 Статистика
     - ⚙️ Настройки

### Тест 2: Административные функции (если вы admin)

1. **Список пользователей**
   ```
   /users
   ```
   Должен показать всех зарегистрированных пользователей

2. **Создание менеджера**
   ```
   /set_manager_role 6208324414 test@rmksib.ru "Тестовый Менеджер"
   ```
   Замените `6208324414` на реальный Telegram ID нужного пользователя

3. **Изменение роли**
   ```
   /set_role 6208324414 manager
   ```

### Тест 3: Настройки и фильтры (GUI меню)

1. **Открыть настройки**
   - Нажмите кнопку "⚙️ Настройки"
   - Должно появиться GUI меню с кнопками

2. **Выбор заказчиков**
   - Нажмите "🏢 Выбрать заказчиков"
   - Выберите несколько заказчиков (клик по названию)
   - Нажмите "✅ Сохранить"

3. **Выбор номенклатуры**
   - Нажмите "🏷 Выбрать номенклатуру"
   - Выберите несколько категорий
   - Нажмите "✅ Сохранить"

4. **Переключение уведомлений**
   - Нажмите "🔔 Включить/Выключить уведомления"
   - Нажмите кнопку переключения

### Тест 4: AI анализ

1. **Анализ лота**
   - Дождитесь, пока парсер создаст тестовые лоты (может занять до 30 минут)
   - Или создайте лот вручную через БД
   - Используйте команду:
     ```
     /analyze PVK-20241029-001
     ```
   - Бот должен вернуть анализ от Perplexity

2. **Отправка анализа на email**
   - В деталях лота нажмите кнопку "📧 Отправить анализ на email"
   - Или используйте команду:
     ```
     /mail_analysis PVK-20241029-001
     ```

### Тест 5: Парсер и уведомления

1. **Проверка работы парсера**
   - Парсер запускается автоматически каждые 30 минут
   - Проверьте логи бота на наличие сообщений "Parser job: created X new lots"

2. **Проверка email уведомлений**
   - Убедитесь, что в `.env` настроены SMTP параметры
   - При создании новых лотов должны прийти письма на указанные email

---

## 📊 Структура проекта

```
zakupki-rmksib/
├── bot/                        # Telegram бот
│   ├── handlers/               # Обработчики команд
│   │   ├── start.py           # /start, /help
│   │   ├── admin.py           # Админские команды
│   │   ├── lots.py            # Работа с лотами
│   │   ├── suppliers.py       # Поставщики
│   │   ├── statistics.py      # Статистика
│   │   ├── settings.py        # Команды настроек
│   │   └── preferences_gui.py # GUI меню настроек
│   ├── keyboards/             # Клавиатуры
│   │   ├── reply.py          # Reply клавиатуры
│   │   └── inline.py         # Inline клавиатуры
│   ├── middlewares/          # Middleware
│   │   ├── auth.py           # Проверка доступа
│   │   └── logging.py        # Логирование
│   └── states/               # FSM состояния
│       └── forms.py          # Состояния
├── database/                 # База данных
│   ├── models.py            # Модели БД (User, Lot, Supplier, UserPreference)
│   ├── repositories/        # Репозитории для работы с БД
│   └── migrations/          # Миграции Alembic
├── services/                # Сервисы
│   ├── ai/                  # AI анализ (Perplexity)
│   ├── parsers/             # Парсеры закупок
│   └── notifications/       # Email уведомления
├── config/                  # Конфигурация
│   └── settings.py         # Настройки из .env
└── scripts/                # Утилиты
    ├── create_admin.py     # Создание администратора
    └── init_users.py       # Инициализация пользователей
```

---

## 🔍 Проверка кода

### Основные компоненты

#### 1. Модели данных (`database/models.py`)
- `User` — пользователи бота
- `Lot` — закупки
- `Supplier` — поставщики
- `UserPreference` — настройки уведомлений

#### 2. Middleware (`bot/middlewares/auth.py`)
- Автоматическая регистрация пользователей
- Проверка доступа по ролям
- Передача `db_user` в handlers

#### 3. Handlers (`bot/handlers/`)
Каждый handler обрабатывает свой набор команд:
- `start.py` — базовая функциональность
- `admin.py` — админские команды (только для admin)
- `lots.py` — работа с лотами, анализ
- `suppliers.py` — база поставщиков
- `statistics.py` — статистика по системе
- `settings.py` — текстовые команды настроек
- `preferences_gui.py` — GUI меню настроек

#### 4. Сервисы (`services/`)
- `ai/perplexity.py` — интеграция с Perplexity API
- `parsers/pavlik.py` — заглушка парсера
- `parsers/job.py` — планировщик парсинга
- `notifications/email.py` — отправка email

---

## 🐛 Возможные проблемы

### Проблема: Бот не отвечает
- Проверьте, что `.env` файл содержит правильный `BOT_TOKEN`
- Проверьте логи: `python -m bot.main`

### Проблема: Ошибка подключения к БД
- Убедитесь, что Docker запущен: `docker compose up -d`
- Проверьте параметры подключения в `.env`

### Проблема: Миграции не применяются
- Выполните: `alembic upgrade head`
- Проверьте, что Docker с PostgreSQL запущен

### Проблема: Email не отправляются
- Проверьте настройки SMTP в `.env`
- Убедитесь, что `PERPLEXITY_API_KEY` заполнен для AI анализа

### Проблема: Команда не работает
- Проверьте, что вы зарегистрированы: `/user_info`
- Для админских команд нужна роль `admin`

---

## 📝 Чек-лист для тестирования

- [ ] Бот отвечает на `/start`
- [ ] Главное меню отображается корректно
- [ ] Все 4 кнопки главного меню работают
- [ ] GUI меню настроек открывается
- [ ] Можно выбрать заказчиков
- [ ] Можно выбрать номенклатуру
- [ ] Уведомления переключаются
- [ ] Админ может создавать менеджеров (`/set_manager_role`)
- [ ] Админ может изменять роли (`/set_role`)
- [ ] AI анализ работает (`/analyze`)
- [ ] Email анализ отправляется
- [ ] Парсер создает лоты (проверить через 30 мин или вручную в БД)
- [ ] Email уведомления приходят

---

## 📞 Контакты для вопросов

Если возникнут вопросы или проблемы:
1. Проверьте логи бота
2. Проверьте `.env` файл
3. Обратитесь к разработчику

---

**Удачи в тестировании!** 🚀


