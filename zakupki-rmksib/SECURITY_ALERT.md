# ⚠️ КРИТИЧЕСКОЕ ПРЕДУПРЕЖДЕНИЕ О БЕЗОПАСНОСТИ

## Проблема

В документации проекта были обнаружены реальные API ключи:
- **Telegram Bot Token** (в файле QUICK_START.md)
- **Perplexity API Key** (в файлах PROJECT_STATUS.md, CURATOR_GUIDE.md, NEXT_SESSION_SUMMARY.md, QUICK_START.md)

Эти ключи были закоммичены в публичный репозиторий GitHub и могли быть скомпрометированы.

## ✅ Что было сделано

1. ✅ Удалены все реальные ключи из файлов документации
2. ✅ Заменены на плейсхолдеры (`your_bot_token_here`, `your_perplexity_api_key_here`)
3. ✅ Обновлен `.gitignore` для предотвращения подобных случаев
4. ✅ Создан новый коммит с исправлениями
5. ✅ Изменения отправлены на GitHub

## 🚨 СРОЧНЫЕ ДЕЙСТВИЯ ТРЕБУЮТСЯ

### 1. Смените Telegram Bot Token

**Немедленно:**

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Выберите "API Token"
5. Выберите "Revoke current token" (отозвать текущий токен)
6. Скопируйте новый токен
7. Обновите `.env` файл с новым токеном

**Старые токены (ОТОЗВАНЫ):**
```
[Все старые токены отозваны через @BotFather]
```

### 2. Смените Perplexity API Key

**Немедленно:**

1. Войдите в [Perplexity AI Dashboard](https://www.perplexity.ai/settings/api)
2. Найдите ваш API ключ
3. Удалите/отзовите текущий ключ
4. Создайте новый API ключ
5. Обновите `.env` файл с новым ключом

**Старые ключи (ОТОЗВАНЫ):**
```
[Все старые ключи отозваны через Perplexity Dashboard]
```

### 3. Проверьте использование ключей

Проверьте логи и активность:
- Telegram Bot: проверьте, не было ли несанкционированного доступа
- Perplexity API: проверьте использование API в dashboard

### 4. Обновите .env файл

После получения новых ключей обновите файл `.env`:

```env
BOT_TOKEN=новый_токен_здесь
PERPLEXITY_API_KEY=новый_ключ_здесь
```

## 📋 Предотвращение в будущем

### Правила безопасности:

1. **НИКОГДА не коммитьте реальные ключи** в репозиторий
2. **Всегда используйте `.env` файл** для хранения секретов
3. **Проверяйте файлы перед коммитом** на наличие секретов
4. **Используйте `.env.example`** с плейсхолдерами
5. **Добавляйте файлы с секретами в `.gitignore`**

### Проверка перед коммитом:

```bash
# Проверьте файлы на наличие реальных ключей
grep -r "pplx-" . --exclude-dir=.git
grep -r "BOT_TOKEN=" . --exclude-dir=.git | grep -v "your_bot_token"
```

### Использование git-secrets:

Рекомендуется установить `git-secrets` для автоматической проверки:

```bash
# Установка (Linux/Mac)
git clone https://github.com/awslabs/git-secrets.git
cd git-secrets
sudo make install

# Настройка
git secrets --install
git secrets --register-aws
git secrets --add 'pplx-[A-Za-z0-9]+'
git secrets --add 'BOT_TOKEN=[0-9]+:[A-Za-z0-9_-]+'
```

## 🔍 Проверка истории Git

Если нужно полностью удалить ключи из истории Git (требует force push):

```bash
# ВНИМАНИЕ: Это перепишет историю Git!
# Используйте только если репозиторий приватный или вы уверены в действиях

# Использование BFG Repo-Cleaner (рекомендуется)
# Или git filter-branch
```

**⚠️ ВАЖНО:** Если репозиторий публичный, ключи уже могли быть скопированы. Обязательно смените их!

## 📞 Контакты

Если обнаружена несанкционированная активность:
1. Немедленно отзовите все ключи
2. Проверьте логи использования
3. При необходимости обратитесь в поддержку сервисов

---

**Дата обнаружения:** 25 ноября 2025  
**Статус:** Исправлено, требуется смена ключей

