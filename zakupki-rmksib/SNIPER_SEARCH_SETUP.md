# Настройка Sniper Search API

## 📋 Обзор

Sniper Search — это сервис для автоматического поиска поставщиков товаров через поисковые системы (Yandex, Google, Bing). API позволяет создавать задачи поиска и получать результаты в виде списка поставщиков с контактами и ценами.

---

## 🔑 Шаг 1: Регистрация и получение API доступа

### 1.1 Регистрация на сайте

1. Перейдите на официальный сайт: [sniper-search.ru](https://sniper-search.ru)
2. Зарегистрируйте аккаунт
3. Свяжитесь с поддержкой для получения доступа к API

### 1.2 Получение API токена

После получения доступа к API вам будет предоставлен:
- **API Token** (токен авторизации)
- **Base URL** (обычно: `https://api.sniper-search.com`)

---

## ⚙️ Шаг 2: Настройка в проекте

### 2.1 Добавление переменных в .env

Откройте файл `.env` в корне проекта и добавьте следующие переменные:

```env
# Sniper Search API
SNIPER_SEARCH_BASE_URL=https://api.sniper-search.com
SNIPER_SEARCH_API_TOKEN=your_api_token_here
```

**Важно:**
- Замените `your_api_token_here` на ваш реальный API токен
- Если Base URL отличается от стандартного, укажите правильный адрес

### 2.2 Проверка настроек

После добавления переменных в `.env`, перезапустите бота:

```bash
python -m bot.main
```

---

## 🔍 Шаг 3: Проверка работы

### 3.1 Тестирование через бота

1. Запустите бота
2. Нажмите кнопку **"🔍 Поиск Поставщиков"**
3. Выберите метод **"Sniper Search"**
4. Введите название товара (например: "Болт М10х50")
5. Дождитесь результата

### 3.2 Ожидаемый результат

При успешной настройке:
- ✅ Создается задача поиска
- ✅ Возвращается `task_id`
- ✅ Поиск выполняется в фоновом режиме

При ошибке:
- ❌ Показывается сообщение об ошибке
- ⚠️ Автоматически переключается на Perplexity AI (fallback)

---

## 🛠️ Шаг 4: Устранение проблем

### Проблема 1: "Cannot connect to host"

**Симптомы:**
```
Cannot connect to host api.sniper-search.com:443 ssl:default [getaddrinfo failed]
```

**Причины:**
- Проблемы с DNS
- Нет доступа к интернету
- Неверный Base URL

**Решение:**
1. Проверьте интернет-соединение
2. Проверьте правильность `SNIPER_SEARCH_BASE_URL` в `.env`
3. Попробуйте выполнить ping: `ping api.sniper-search.com`
4. При проблемах система автоматически переключится на Perplexity AI

### Проблема 2: "API error: 401" или "Authorization failed"

**Симптомы:**
```
API error: 401 - Unauthorized
```

**Причины:**
- API токен не установлен
- API токен неверный или устарел
- API токен не имеет нужных прав доступа

**Решение:**
1. Проверьте, что `SNIPER_SEARCH_API_TOKEN` установлен в `.env`
2. Убедитесь, что токен скопирован полностью (без пробелов)
3. Проверьте срок действия токена
4. Обратитесь в поддержку Sniper Search для получения нового токена

### Проблема 3: "API error: 403" или "Forbidden"

**Симптомы:**
```
API error: 403 - Forbidden
```

**Причины:**
- Недостаточно прав доступа у токена
- Превышен лимит запросов
- Аккаунт заблокирован

**Решение:**
1. Проверьте лимиты вашего тарифа
2. Обратитесь в поддержку Sniper Search
3. Используйте Perplexity AI как альтернативу

### Проблема 4: "API error: 404" или "Not Found"

**Симптомы:**
```
API error: 404 - Not Found
```

**Причины:**
- Неверный Base URL
- Изменился адрес API
- Неверный endpoint

**Решение:**
1. Проверьте актуальный Base URL в документации Sniper Search
2. Обновите `SNIPER_SEARCH_BASE_URL` в `.env`

---

## 📚 Дополнительная информация

### Как работает Sniper Search в боте

1. **Создание задачи поиска:**
   - Пользователь вводит название товара
   - Бот отправляет запрос в Sniper Search API
   - API создает задачу и возвращает `task_id`

2. **Выполнение поиска:**
   - Поиск выполняется в фоновом режиме на стороне Sniper Search
   - Используются поисковые системы: Yandex, Google, Bing
   - Ищутся поставщики в России (geo_targets: 225)

3. **Получение результатов:**
   - Результаты доступны через некоторое время
   - Для получения результатов используется `task_id`
   - Результаты включают контакты поставщиков и цены

### API методы, используемые в боте

1. **POST `/api/v0/requests/wizard`** — создание задачи поиска
2. **GET `/api/v0/requests`** — получение статуса задачи
3. **GET `/api/v0/mailbox/replies`** — получение ответов поставщиков

### Параметры поиска (по умолчанию)

- **Поисковые системы:** Yandex, Google, Bing
- **Геотаргетинг:** Россия (225)
- **Язык:** Русский (1)
- **Типы поставщиков:** Производство, Опт, Торговля, Услуги
- **Парсинг рекламы:** Включен
- **Количество страниц:** 3

---

## 🔄 Автоматический fallback

Если Sniper Search недоступен, система автоматически переключается на **Perplexity AI**:

1. При сетевых ошибках (DNS, подключение)
2. При таймауте запроса (>10 секунд)
3. При ошибках API (кроме 401/403)

Пользователь увидит сообщение:
```
⚠️ Sniper Search недоступен
Использую Perplexity AI для поиска
```

---

## 📞 Контакты поддержки

- **Сайт:** [sniper-search.ru](https://sniper-search.ru)
- **Документация API:** (уточните у поддержки)
- **Email поддержки:** (уточните у поддержки)

---

## ✅ Чеклист настройки

- [ ] Зарегистрирован аккаунт на sniper-search.ru
- [ ] Получен доступ к API
- [ ] Получен API токен
- [ ] Добавлен `SNIPER_SEARCH_BASE_URL` в `.env`
- [ ] Добавлен `SNIPER_SEARCH_API_TOKEN` в `.env`
- [ ] Перезапущен бот
- [ ] Протестирован поиск через бота
- [ ] Проверена работа fallback на Perplexity

---

**Дата создания:** 19 ноября 2025  
**Версия:** 1.0

